{% extends 'base.html.twig' %}

{% block title %} {{ title }} {% endblock %}

{% block body %}
<h1> {{ title }} </h1>

<h2>Tag individuels</h2>

<h3> {{ tag.name }} (id {{tag.id}}) </h3>
<p>
{{ tag.description }}
</p>

<h3> {{ cssTag.name }} (id {{cssTag.id}}) </h3>
<p>
{{ cssTag.description }}
</p>

<h2>Liste de tout les Tags</h2>
{% for tag in tags %}
<h3> {{ tag.name }} (id {{tag.id}}) </h3>
<p>
{{ tag.description }}
</p>
{% endfor %}

<h2>Liste des Tags SANS description</h2>

{% for tag in nullDescriptionTags %}
<h3> {{ tag.name }} (id {{tag.id}}) </h3>
<p>
{{ tag.description }}
</p>
{% endfor %}

<h2>Liste des Tags AVEC description</h2>

{% for tag in notNullDescriptionTags %}
<h3> {{ tag.name }} (id {{tag.id}}) </h3>
<p>
{{ tag.description }}
</p>
{% endfor %}


<h2>filtre avec html</h2>
{% for tag in keywordTags1 %}
<h3> {{ tag.name}} (id {{tag.id}} ) </h3>
<p> {{tag.description}} </p>
{% endfor %}

<h2>filtre avec exercitationem</h2>
{% for tag in keywordTags2 %}
<h3> {{ tag.name}} (id {{tag.id}} ) </h3>
<p> {{tag.description}} </p>
{% endfor %}

{# {% for tag in keywordTags3 %}
<h2>filtre avec foo</h2>
<h3> {{ tag.name}} (id {{tag.id}} ) </h3>
<p> {{tag.description}} </p>
{% endfor %} #}

<h2>Liste des tags d'une SchoolYear</h2>
{% for tag in schoolYearTags %}
<h3> {{ tag.name}} (id {{tag.id}} ) </h3>
<p> {{tag.description}} </p>
{% endfor %}

<h2>Liste des student du tag 4</h2>
<ul>
{% for student in tag.students %}
    <li>{{ student.firstName }} {{ student.lastName }} (id {{student.id}} )</li>
{% endfor %}
</ul>
{% endblock %}
